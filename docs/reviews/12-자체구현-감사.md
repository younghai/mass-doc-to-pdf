# 자체 구현 감사 리포트 — 브랜치 `review/expert-review-p0-1`

> 작성일: 2026-07-05 · 종합: Claude(Fable 5, 감사) · 감사 수행: **독립 감사 에이전트 2개**(코드 정확성 + 보안), 자기 승인 방지 목적
> 대상: 이번 세션에서 직접 구현한 10커밋(코드 5 + 문서 5) vs 베이스 `8f83b99`
> 방법: 감사관이 diff/소스 정독 + **테스트를 직접 실행** + 신규 테스트의 **레드-그린을 소스 되돌림으로 실증** + CSP를 앱 실제 리소스와 대조. 추측 없이 파일:라인 근거.

## 종합 판정: **병합 가능 (Mergeable) · 배포 차단 항목 없음**

- 코드 커밋 5개 **전부 SOUND**, 보안 변경 6개 중 **5 CLOSED / 1 PARTIAL**(핵심 취약점은 닫힘, 잔여는 이번 범위 밖 기존 벡터).
- 실측 검증(재현 가능): `pnpm -r typecheck` 3패키지 clean · API **143 passed/1 skipped** · web **29 passed** · config **22 passed**.
- 신규 테스트 2건(품질게이트·삭제확인) 레드-그린 실증: 각각 수정 제거 시 RED(`expected 'success' to be 'failed'`, `expected confirm to be called`) → 복원 시 GREEN, `git diff` 공백으로 원본 무손상 확인.

---

## 1. 코드 정확성 감사 (요약)

커밋별 판정 — 전부 **SOUND**, 회귀 없음:

| 커밋 | 판정 | 핵심 근거 |
|---|---|---|
| 692fb80 품질게이트 통일 | SOUND | 인라인/큐가 `quality.ts`의 동일 predicate import(사설 복제 제거). 비재시도 분류 체인(`quality_gate_failed`→`PERMANENT_REASONS`→`isPermanentFailure`) 완전 추적, 테스트 assertion(attempts=0/lockedAt=null/PDF 미저장/report 저장) 코드와 일치 |
| 1b78cd4 보안 기본선 | SOUND | `parseTrustProxy` 전 분기 실측, `trustProxy: deps.trustProxy ?? 1`·멀티파트 제한·nginx 헤더(server 스코프 상속)·README 정정 확인 |
| e895a73 삭제 확인 | SOUND | 3개 삭제 지점(JobsTable 1 + JobDetail 2) 전부 가드, 레드-그린 실증 |
| f7b9247 배포/패키징 하드닝 | SOUND | 토큰 stdin화, `.dockerignore` negation 순서 정확, package.sh 가드 정규식 실측 |
| 89ade92 config 중앙화 | SOUND | import-time env 제거→config 주입, 429 비교식·기본값 불변 |

**특별 확인 3건 충족:** ①품질게이트 인라인/큐 동일 판정(각 경로 테스트 존재) ②삭제 확인 3개 지점 전부 ③MAX_ACTIVE 중앙화가 429 동작 보존.

---

## 2. 보안 감사 (요약)

| 항목 | 판정 | 근거 |
|---|---|---|
| trustProxy (SEC-01) | **CLOSED** | `trustProxy:1`이 XFF 최우측(nginx가 넣은 실 client IP)만 신뢰 → 클라이언트 위조 XFF로 per-IP rate limit 우회 차단. `@fastify/rate-limit` 키=`req.ip` 확인, nginx `$proxy_add_x_forwarded_for` 1홉 정합 |
| 멀티파트 제한 (SEC-05) | **CLOSED** | 정상 요청은 파일 1파트(qualityMode는 쿼리스트링)라 `files:1/fields:8/parts:12` 넉넉. 파트 폭탄 DoS 차단 |
| nginx 보안 헤더/CSP (SEC-02) | **CLOSED** | server 스코프 `always`가 두 location에 상속. **CSP가 앱 실제 리소스와 정합** — recharts 인라인 스타일(style-src 'unsafe-inline' 필요), 프리뷰 img same-origin, 녹음 blob(media-src blob:), fetch same-origin(connect-src 'self'). 과조임/과느슨 아님 |
| deploy-ssh 토큰 (SEC-03) | **PARTIAL** | 토큰 stdin화로 원격 cmdline 노출 제거(CLOSED). 단 동일 파일에 `image_tag` 인젝션 벡터 잔존(아래 백로그) |
| .dockerignore (SEC-10) | **CLOSED** | negation 순서 정확(실 시크릿 제외·예제 재포함). 제품 Dockerfile은 `COPY . .` 미사용이라 방어심층 |
| package.sh 가드 (PO-08) | **CLOSED** | newsletter/odysseus/node_modules/실시크릿 재유출 실측 차단(대소문자·경로위치 우회 방어) |

**결과:** Critical 0 · High 0 · Medium 1(기존 잔존) · Low/Info 3.

---

## 3. 후속 백로그 (비차단 — Codex 수정 트랙)

> 아래는 감사가 발견한 개선 항목입니다. **배포 차단이 아니며**, 각각 Codex 구현 스펙으로 전환 가능(구현은 Codex, 감사는 Fable 5). 우선순위 순.

| # | 심각도 | 항목 | 위치 | 권장 조치 |
|---|---|---|---|---|
| AUD-1 | **MEDIUM** | `deploy-ssh.yml` `image_tag` 셸 인젝션 (기존 벡터, 이번 PR 범위 밖·동일 파일) | `.github/workflows/deploy-ssh.yml:76` | `image_tag`를 `env:`로 받아 원격 `'$IMAGE_TAG'` 참조, 또는 `^[A-Za-z0-9._:-]+$` 검증. write 권한자 한정이라 차단은 아님 |
| AUD-2 | LOW | 보안 변경 E2E 커버리지 공백 (trustProxy req.ip / 멀티파트 parts 거부 통합 테스트 부재) | `apps/api/src/app.test.ts` | Fastify inject로 위조 XFF→req.ip 무시, 초과 parts→413/제한 확인 테스트 추가 |
| AUD-3 | LOW | `/api/convert` 429 한도 통합 테스트 부재 (리팩터 전후 모두) | `apps/api/src/routes/convert.ts:141-144` | 활성 job ≥ 한도 시 429 통합 테스트 1건 |
| AUD-4 | LOW | JobDetail **success-브랜치** 삭제 확인 미커버 (failed 브랜치만 테스트) | `apps/web/src/pages/JobDetail.tsx:158` | success 상태 삭제 버튼 확인 테스트 1건 |
| AUD-5 | LOW | README 문서 드리프트 — "2026-06-11" 이력 표에 옛 `trustProxy: true` 잔존 | `README.md:308` | "기본 1홉(2026-07-05 정정)"으로 갱신 또는 이력임을 명시 |
| AUD-6 | LOW | package.sh 가드 정규식이 `.env.example`도 flag (현재는 tar가 예제를 제외해 무발생, 향후 exclude 완화 시 오탐) | `standalone/scripts/package.sh:57` | `grep -v '\.example$'` 예외 또는 패턴에 예제 접미사 제외 |
| AUD-7 | INFO | CSP `img-src`의 `lh3.googleusercontent.com` 현재 미사용(아바타 `<img>` 미렌더) | nginx confs | 아바타 도입 시 유지, 아니면 제거 — 선택 |

---

## 4. 결론
- 이번 세션의 10커밋은 **자기 승인 없이 독립 검증**을 통과했다(코드 SOUND·보안 CLOSED, 레드-그린 실증).
- **병합/배포 차단 항목 없음.** AUD-1(MEDIUM)은 이번 수정이 표적한 취약점과 별개의 기존 벡터이므로 후속 처리 권고.
- 후속 백로그(AUD-1~7)는 [10-codex-구현-스펙](10-codex-구현-스펙.md) 형식으로 Fable 5가 스펙화 → Codex 구현 → Fable 5 감사 사이클로 처리.
